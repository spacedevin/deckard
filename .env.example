# Copy to .env — never commit .env
# Serverless inference (chat) — https://inference.do-ai.run
GRADIENT_MODEL_ACCESS_KEY=

# DigitalOcean control API (key management, etc.) — not used for inference
DIGITALOCEAN_API_TOKEN=
