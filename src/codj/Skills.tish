// AI lane line policy — single source: StreamDemo.tish (coDjLineAllowedOnLane).

import { coDjLineAllowedOnLane } from './StreamDemo.tish'

export fn skillAllowsLine(line, laneId) {
  return coDjLineAllowedOnLane(line, laneId)
}
