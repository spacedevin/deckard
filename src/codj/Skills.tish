// Skill-based line validation — single source: StreamDemo.tish (coDjLineAllowedForSkills).

import { coDjLineAllowedForSkills } from './StreamDemo.tish'

export fn skillAllowsLine(line, skillIds) {
  return coDjLineAllowedForSkills(line, skillIds)
}
