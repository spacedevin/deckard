{
  "name": "tish-midi",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal FL-style web DAW in Tish",
  "scripts": {
    "build": "tish compile --target js --jsx vdom src/main.tish -o dist/bundle.js",
    "build:counter": "tish compile --target js src/tishact/examples/counter-main.tish -o dist/counter-bundle.js",
    "serve": "npx --yes serve . -p 3456",
    "gateway": "cd ../tish && cargo run -p tish -q --features ws,process -- run ../tish-midi/services/gateway/main.tish",
    "agent": "tish run services/agent-worker/main.tish",
    "token-demo": "node services/token-stream-demo/index.js"
  }
}
